---
import { SITE_CONFIG } from "../config";

const isHome = Astro.url.pathname === "/";
const justifyStyle = isHome ? "justify-end" : "justify-between";
---

<footer class=`flex ${justifyStyle} items-center py-4 mt-8`>
	{
		!isHome && (
			<a href="/" class="text-xl md:text-2xl">
				Home
			</a>
		)
	}
	<div class="flex space-x-4">
		<a
			href="/rss.xml"
			class="text-sm"
			target="_blank"
			rel="noopener noreferrer"
		>
			RSS
		</a>
		<span>/</span>
		<a
			href={`mailto:${SITE_CONFIG.author.email}`}
			target="_blank"
			rel="noopener noreferrer"
			class="text-sm"
		>
			Mail
		</a>
		<span>/</span>
		<a
			href={`https://github.com/${SITE_CONFIG.github.username}`}
			class="text-sm"
			target="_blank"
			rel="noopener noreferrer"
		>
			GitHub
		</a>

		<style>
			footer a {
				font-family: var(--font-atkinson);
				font-size: clamp(16px, 3vw, 14px);
				padding: 0 0.15rem;
				margin: 0 -0.15rem;
			}

			:global(html:not(.dark)) footer a:hover {
				background-color: #0000ee;
				color: #ffff11;
			}

			:global(html.dark) footer a:hover {
				background-color: var(--color-logo-dark);
				color: #111827;
			}
		</style>
	</div>
</footer>
